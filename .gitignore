# dependencies
node_modules
.pnp
.pnp.js

# next.js
.next
out
/build
/dist

# env (NEVER commit secrets)
.env
.env.local
.env.development.local
.env.test.local
.env.production.local
.env*.local

# vercel
.vercel

# typescript
*.tsbuildinfo
next-env.d.ts

# supabase
supabase/.branches
supabase/.temp

# misc
.DS_Store
*.pem
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.turbo

# editor
.vscode/*
!.vscode/settings.json
!.vscode/extensions.json
.idea
.claude/

# macOS Finder / iCloud creates "* 2.*" duplicates when files are moved on
# synced folders; never commit these.
* 2.*

# testing
coverage

# prototype scratch data
prototypes/**/data/

# sharedresearch_ingester exclusions
sharedresearch_ingester/.DS_Store
sharedresearch_ingester/.venv
sharedresearch_ingester/.pytest_cache
sharedresearch_ingester/logs/
sharedresearch_ingester/**/__pycache__/
sharedresearch_ingester/data/

# local-only docs: working notes, brainstorm specs, handoff. Anything that
# should NOT travel with the repo lives here or in HANDOFF.md.
local/
HANDOFF.md
